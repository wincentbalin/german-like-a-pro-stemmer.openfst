import 'symbols.grm' as sym;
to_lowercase = sym.to_lowercase*;
synonymliste = StringFile['synonymliste', 'utf8', 'utf8'];
export SYNONYMLISTE = Optimize[Invert[to_lowercase] @ synonymliste @ to_lowercase];

